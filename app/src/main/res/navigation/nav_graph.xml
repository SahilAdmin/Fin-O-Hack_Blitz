<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/frag_main">

    <fragment
        android:id="@+id/frag_main"
        android:name="com.admin_official.det.frag_main"
        android:label="frag_main"
        tools:layout="@layout/frag_main" >
        <action
            android:id="@+id/action_frag_main_to_frag_history"
            app:destination="@id/frag_history" />
    </fragment>
    <fragment
        android:id="@+id/frag_history"
        android:name="com.admin_official.det.frag_history"
        android:label="frag_history"
        tools:layout="@layout/frag_history" >
        <action
            android:id="@+id/action_frag_history_to_frag_main"
            app:destination="@id/frag_main" />
    </fragment>
</navigation>